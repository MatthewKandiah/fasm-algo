# insertion sort

## usage
I haven't worked out passing command line arguments yet, so inputs will be passed in from `data.txt` and compiled into the executable (so updating the inputs requires recompiling). This file is expected to contain single-digit base-10 integers with no whitespace or special characters. Numbers should then be printed to stdout sorted into ascending order.

